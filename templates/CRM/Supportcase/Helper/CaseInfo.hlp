{htxt id='id-about-clients-updating'}
  <p>{ts}If you change client system adds this case to the trash and create the same new case(with the same activities and tags) with new client.{/ts}</p>
{/htxt}
