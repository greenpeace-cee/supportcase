{htxt id='id-about-clients-updating'}
  <p>{ts}If you change the client, CiviCRM will create re-create this case (with the same activities and tags) for the new client and delete the old one.{/ts}</p>
{/htxt}
